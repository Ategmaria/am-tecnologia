
import React from "react";
import ReactDOM from "react-dom/client";
import AMTecnologia from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AMTecnologia />
  </React.StrictMode>
);
